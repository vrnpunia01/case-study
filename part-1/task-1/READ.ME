A. Write an ansible playbook for installing nginx, docker, logrotate. 

B. Ensure nginx container is running on port 8080 on host and log rotation is cleaning the logs of stdout of nginx container once it reaches 100mb.

Note: (Separate roles should be created for the tasks mentioned inline) 
